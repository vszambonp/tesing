# REST API GUIDE

##To start the project, do:

``` 
$ cd [root directory]
$ composer install
$ php -S localhost:8000
```

##Routes

HTTP Method | Route
------------ | -------------
    POST     |   auth/login
Content in the first column | Content in the second column

